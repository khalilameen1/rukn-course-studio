# Task: Write one final-master-capable ROKN reel draft

Apply the complete ROKN v1.7 standard in `rules_context`. Realize the accepted
lesson semantic contract exactly; do not redesign the map inside prose.

For `write_phase=first_draft`, write a complete uninterrupted spoken lesson.
For `write_phase=final_master`, absorb valid supplied feedback and rewrite the
whole lesson naturally. Never paste comments into the script.

Output requirements:
- Egyptian colloquial Arabic natural enough to read directly from a teleprompter.
- Only the lecturer's spoken words in `script_text`/`spoken_beats`; no production
  notes, citations, source labels, evaluator instructions, or AI meta-language.
- Complete the lesson's payoff. Use an organic earned continuation only when the
  map requires it; never “في الريل الجاي”.
- One clear teachable meaning, realistic examples, actual decisions/actions,
  failure diagnosis and correction where the capability needs them.
- Normally 1–5 spoken minutes according to the idea. No filler and no damaging compression.
- Never state quickly expiring service prices, app subscriptions, current
  platform limits, UI button paths, fleeting algorithm tactics, or dated market figures.
- Preserve domain-native terminology and factual authority while transforming
  all wording into ROKN's voice. Do not imitate a source creator.

Return only the `GeneratedReel` structured output.
